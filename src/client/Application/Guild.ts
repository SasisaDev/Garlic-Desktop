import Channel from "./Channel";

export default class Guild {
    private ID: string;

    GetChannel(id: string): Channel {
        return null;
    }

    GetChannels(): Channel[] {
        return null;
    }
}