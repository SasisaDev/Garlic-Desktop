import React, { useEffect } from "react"

import "../../API/User"

import "./Profile.scss"

export default function Profile({user}) {

    useEffect(()=>{
        
    }, [])

    return (
        <div>

        </div>
    )
}